import styled from '@emotion/styled'
import { colors } from '../../styles';

export const MySelectContainer = styled.select`
  width: 100%;
  height: 40px;
  border: solid 1px ${colors.grayLight};
  border-radius: 5px;
  padding-left: 10px;
`;
